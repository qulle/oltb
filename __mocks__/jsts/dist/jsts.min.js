import { jest } from '@jest/globals';

export const io = {
    OL3Parser: jest.fn().mockImplementation(() => {
        return {
            parse: jest.fn().mockReturnValue('jest'),
            inject: jest.fn()
        };
    })
};