import { describe, it, expect } from '@jest/globals';

describe('Measurements', () => {
    it('should get area', () => {
        expect(1).toEqual(1);
    });

    it('should get length', () => {
        expect(1).toEqual(1);
    });
});